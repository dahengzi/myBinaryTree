#include "myBinaryTree.h"
#include <vector>
#include <list>
#include <stack>

void LeftChildTraverse(myBTreeNodeP node)
{
    stack<myBTreeNodeP> bstack;
    myBTreeNodeP p = node;
    while(p!=NULL)
    {
        bstack  
    }
}
